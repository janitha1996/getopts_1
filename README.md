# lab02_getopts.sh  
Bash script demonstrating option parsing using `getopts`.  
The script detects valid flags, invalid flags, duplicate flags, and unexpected positional arguments.

---

## 📌 Usage

```bash
./lab02_getopts.sh [options] [args]
